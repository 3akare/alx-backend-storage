// Returns the number of documents in the school collection
// db.school.count() is deprected, so i would use db.school.countDocuments()
// which provides a better performance
db.school.countDocuments()
